version: '3.8'
services:
  # ... (các dịch vụ backend đã có)

  my-fe-app-service:
    container_name: voicebe-frontend
    build:
      context: ./fe  # HOẶC thay bằng đường dẫn thư mục code UI của bạn
      dockerfile: Dockerfile
    ports:
      - "3000:80"  # Truy cập FE qua http://localhost:3000
    depends_on:
      - my-java-app-service # FE chỉ cần đợi BE khởi động (có thể bỏ nếu không cần)