version: '3.8'
# Trong file docker-compose.yml
# Trong file docker-compose.yml trên GitHub
services:
  # Vẫn giữ định nghĩa các dịch vụ BE và DB (nhưng không khởi động)
  # ... mongodb, my-java-app-service ...

  my-fe-app-service:
    container_name: voicebe-frontend
    build:
      context: .
      dockerfile: Dockerfile
      args:
        # Giữ lại VITE_API_URL, nhưng dùng giá trị placeholder hoặc địa chỉ thật nếu có
        VITE_API_URL: http://placeholder-api.com
    ports:
      - "3000:80"
    # DÒNG depends_on ĐÃ BỊ XÓA (để tránh lỗi undefined service)